{
  "name": "interval-missing-side-nav-mre",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon -r dotenv/config -r tsconfig-paths/register --watch src -e ts src/index.ts",
    "build": "tsc",
    "lint": "eslint .",
    "test": "echo \"Nothing here\""
  },
  "dependencies": {
    "@interval/sdk": "^0.37.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.45",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.2",
    "typescript": "4.5.5"
  }
}
